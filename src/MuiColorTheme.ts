import {createTheme} from "@mui/material";

export const muiColorTheme = createTheme({
  palette: {
    primary: {
      main: '#1565C0',
    },
    secondary: {
      main: '#616161',
    },
  },
});